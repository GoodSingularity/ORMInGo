CREATE TABLE Company (
    ID uuid DEFAULT gen_random_uuid(),
    name       text,
    arr        text[],                                                                                                                             power      int,
    PRIMARY KEY (ID)
);
